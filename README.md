# Web-project

In order to clone the project you need to add private key (located in server ~/.ssh) to ssh-agent, steps:
*call "%GIT_INSTALL_ROOT%/cmd/start-ssh-agent.cmd" //Start ssh-agent
*ssh-add /Users/Kasparas/id_rsa //Adds private key

